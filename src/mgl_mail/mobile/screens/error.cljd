(ns mgl-mail.mobile.screens.error
  (:require 
   ["package:flutter/material.dart" :as m]
   ["package:mongol/mongol.dart" :as mgl]
   [cljd.flutter :as f]))
   

(defn screen
  []
  (f/widget
   (m/Center)
   (m/Padding
    .padding (m.EdgeInsets/all 8))
   (mgl/MongolText "error")))