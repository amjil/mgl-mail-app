(ns mgl-mail.screens.error
  (:require 
   ["package:flutter/material.dart" :as m]
   ["package:mongol/mongol.dart" :as mgl]
   [cljd.flutter :as f]))
   

(defn screen
  [message]
  (f/widget
   (m/Center)
   (m/Padding
    .padding (m.EdgeInsets/all 8))
   (mgl/MongolText message)))