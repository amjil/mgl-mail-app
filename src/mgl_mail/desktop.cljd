(ns mgl-mail.desktop
  (:require
   ["package:flutter/material.dart" :as m]
   [cljd.flutter :as f]))
   
(defn app []
  (f/widget
   (m/MaterialApp
    .theme (m/ThemeData .primarySwatch m.Colors/pink))
   .home
   (m/Scaffold)
   .body
   (m/Center)
   (m/Text "x")))